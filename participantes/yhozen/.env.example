# Optional when the static client and multiplayer server use different hosts.
# http/https values are upgraded to ws/wss and `/multiplayer` is appended when
# no endpoint path is present.
VITE_MULTIPLAYER_URL=wss://rooms.example.com
